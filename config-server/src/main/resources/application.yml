server:
  port: 8071

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Jeffers199817/PROYECTO-TIENDA-ELECTRODOMESTICOS-MICROSERVICIOS.git
          search-paths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}